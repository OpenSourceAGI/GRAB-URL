# grab-url · Reactive Loading (Vue Options API)

The same search-as-you-type list as the `<script setup>` example, written with
the Options API. Everything `data()` returns is already reactive, so
`this.userResults` goes straight into grab's `response` option.

```bash
npm install
npm run dev
```
