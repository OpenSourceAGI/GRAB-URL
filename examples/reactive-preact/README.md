# grab-url · Reactive Loading (Preact)

The React example, 3kB lighter. `useState`'s setter from `preact/hooks` goes
straight into grab's `response` option, so `isLoading`/`error`/data updates land
in component state without a data-fetching library.

```bash
npm install
npm run dev
```
